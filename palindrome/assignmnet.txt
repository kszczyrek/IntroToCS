Implement an algorithm that, for a given number n and base p, checks whether the number is a palindrome in that base (i.e., it reads the same forward and backward).  

The algorithm should not store the digits of the number explicitly but should only use arithmetic operations on integers.
